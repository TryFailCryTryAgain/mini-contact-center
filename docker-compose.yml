services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      - JWT_SECRET=dev-secret-change-in-prod
    volumes:
      - ./backend/src:/app/src   # optional: live-reload in dev

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"